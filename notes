1. Does the raspberry pi need to get response after classification (does it use the result to seperate the material, if so into how many bins - so it has to be synchronous not asynchronous then how is kafka used which seems to support asynchronous)
